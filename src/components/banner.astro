---
interface Props {
    logo: string;
}

const { logo } = Astro.props;
---

<video class="w-full aspect-Default hidden md:block max-lg:hidden" src ={logo} autoplay muted loop >Banner não disponivel</video>