---
interface Props {
    logo: string;
}

const { logo } = Astro.props;
---

<video class="w-full aspect-Default hidden md:block -mt-36 -z-10 max-lg:hidden" src ={logo} autoplay >Banner não disponivel</video>